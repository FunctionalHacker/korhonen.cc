steps:
  build:
    image: klakegg/hugo:ext-asciidoctor-ci
    environment:
      - GOROOT=/usr/local/lib/go
    commands:
      #      - rm -r /korhonen.cc/*
      - npm install
      - hugo -d /korhonen.cc
    volumes:
      - /var/www/korhonen.cc:/korhonen.cc
