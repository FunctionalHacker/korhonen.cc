steps:
  build:
    image: klakegg/hugo:ext-asciidoctor-ci
    commands:
      - rm -r /korhonen.cc/*
      - npm install
      - hugo -d /korhonen.cc
    volumes:
      - /var/www/korhonen.cc:/korhonen.cc
